# Flutter/Dart generated
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.g.dart
*.freezed.dart

# android/ is committed (build reproducibility - see android/.gitignore for
# what's excluded within it: .gradle, local.properties, keystores). The other
# platform folders stay out - this project only builds for Android.
/ios/
/linux/
/macos/
/windows/
/web/

# Local secrets / config
.env
*.jks
key.properties

# IDE
.vscode/
.idea/
*.iml
